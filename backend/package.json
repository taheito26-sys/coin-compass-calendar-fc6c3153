{
  "name": "cryptotracker-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute crypto-tracker --local --file=../seed/schema.sql && wrangler d1 execute crypto-tracker --local --file=../seed/assets.sql",
    "db:init:remote": "wrangler d1 execute crypto-tracker --remote --file=../seed/schema.sql && wrangler d1 execute crypto-tracker --remote --file=../seed/assets.sql",
    "test:local": "bash test-local.sh"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250301.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
